// ABA Tracker - Student Behavior Tracking Application

// Initialize the app
function init() {
    console.log('ABA Tracker initialized');
}

// Initialize when DOM is loaded
document.addEventListener('DOMContentLoaded', init);

